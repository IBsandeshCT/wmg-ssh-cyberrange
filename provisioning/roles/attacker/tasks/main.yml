---
# Kali attacker workstation — no provisioning needed.
# Kali ships with hydra, nmap, ssh and sshpass pre-installed.
# Internet access is unavailable during CyberRangeCZ provisioning.

- name: Confirm attacker is reachable
  ansible.builtin.ping:
